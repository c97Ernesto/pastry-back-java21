# ================================================
# Malva Pastry Shop - Variables de Entorno
# ================================================
# Copia este archivo como .env y ajusta los valores.
# NUNCA subas el archivo .env al repositorio.
# ================================================

# --- Spring Profile ---
# dev  = DataSeeder activo, logs DEBUG, show-sql true
# prod = DataSeeder desactivado, logs WARN, show-sql false
SPRING_PROFILES_ACTIVE=dev

# --- Server ---
SERVER_PORT=8080

# --- Database ---
DB_URL=jdbc:postgresql://localhost:5432/malva_pastry_db
DB_USERNAME=postgres
DB_PASSWORD=tu_password_seguro_aqui

# --- JPA / Hibernate ---
# Valores para produccion: validate, false, false
JPA_DDL_AUTO=update
JPA_SHOW_SQL=true
JPA_FORMAT_SQL=true

# --- JWT ---
# Genera un secreto seguro en Base64 (minimo 64 bytes para HS512):
#   openssl rand -base64 86
JWT_SECRET=REEMPLAZAR_CON_UN_SECRETO_BASE64_SEGURO
JWT_EXPIRATION_MS=86400000

# --- Google OAuth2 ---
GOOGLE_CLIENT_ID=tu_google_client_id_aqui

# --- CORS ---
# Origenes separados por coma
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# --- File Upload ---
UPLOAD_DIR=./uploads/images/products

# --- Thymeleaf ---
THYMELEAF_CACHE=false

# --- Logging ---
# Niveles: TRACE, DEBUG, INFO, WARN, ERROR
LOG_LEVEL_SECURITY=DEBUG
LOG_LEVEL_APP=DEBUG
